<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             x:Class="Region.Mapa">
    <ContentPage.Content>
        <StackLayout>
            <maps:Map x:Name="Maps" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" IsShowingUser="True"/>
            
            
            <Label x:Name="txtLati"  Text="{Binding latitud}" TextColor="Black" IsVisible="False" Margin="15"/>
            <Label x:Name="txtLong"  Text="{Binding longitud}" TextColor="Black" IsVisible="False" Margin="15"/>
            <Label x:Name="txtNomp"  Text="{Binding nombre}" TextColor="Black" Margin="15"/>
            <Label x:Name="txtCapital" Text="{Binding pcapital}" TextColor="Black" Margin="15"/>
            <Label x:Name="txtzona"  Text="{Binding zonah2}" TextColor="Black" Margin="15"/>
            <Label x:Name="txtpob"  Text="{Binding poblacion}" TextColor="Black" Margin="15" IsVisible="False"/>
            <Label x:Name="txtarea"  Text="{Binding area2}" TextColor="Black" Margin="15"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>